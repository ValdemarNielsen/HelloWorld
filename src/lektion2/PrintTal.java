package lektion2;

public class PrintTal {
    public static void main(String[] args) {

        // Alle dele er sat som noter, så de ikke kører samtidig. fjern /* og */ for at få det enkelte program til at køre.

        // Printer alle tal 0-100
     /*   int t = 0;
        while (t < 101) {
            System.out.println(t);
            t = t + 1;
        } */

        // Printer alle tal mellem 2 og 100 som kan divideres med 2.
        /* int t = 2;
        while (t < 101) {
           if (t % 2 == 0) {
                System.out.println(t);
            t = t + 1;
          }              } */

// Systemet printer summen af alle tal mellem 0 og 100.

       /* int t = 0;
        int sum = 0;
        while (t < 101) {

            sum = sum + t;
            t = t+1;
        }   System.out.println(sum); */
}
}